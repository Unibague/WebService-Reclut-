name: deploy WebService Reclut
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: 
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
  env:
    WORKSPACE_PATH: "POR VERIFICAR"
  steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install Openconnect
    
